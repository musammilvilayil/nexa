# Core (required)
httpx>=0.28,<1

# Computer-Use (optional — install for screen/mouse/keyboard control)
# mss>=10.0                  # screen capture
# pynput>=1.8                # mouse and keyboard control
# pywinauto>=0.6             # Windows UI Automation
# Pillow>=11.0               # image processing

# Voice (optional — install for voice interaction)
# vosk>=0.3                  # offline speech-to-text
# sounddevice>=0.5           # microphone capture
# pyttsx3>=2.90              # offline text-to-speech
# edge-tts>=7.0              # online TTS (Microsoft)

# Browser (optional — install for browser automation)
# playwright>=1.50           # browser automation (run: playwright install)
